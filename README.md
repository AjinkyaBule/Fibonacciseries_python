## Fibonacciseries_python
#AIM:Python code for printing Fibonacci series consisting of user entered number of terms.
ALGORITHM:
START
1) Accept number of terms that fibonacci series must consist from user(n)
2) Check whether it is less than or equal to 0(if yes=> print("enter positive value only"))
3) if n==1, print(1).
4) Else, a=0 and b=1
   for i in range(0,n)
   print(a)
   c=a+b;
        a=b;
        b=c;
  END
  


